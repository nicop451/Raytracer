#pragma once

#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>
#include <cstring>
#include <ErrorUtility.h>

namespace nlib 
{
    namespace fileReading {

        std::string shaderSource(std::string& shaderSourcePath);
        void objData();

    }
    namespace fileWriting {

    }
}